dist: trusty
language: node_js
sudo: required
node_js:
  - "6"
cache:
  directories:
    - node_modules

before_install:
  - "npm config set spin false"
  - "npm install -g npm@^2"

install:
  - npm install

script:
  - npm run build
