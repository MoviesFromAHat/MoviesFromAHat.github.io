# Contributing to the app / movie list.

## Running locally

* Clone the repo
* In your local repo, run:
  * `npm install`
  * `npm start`
* Navigate to http://localhost:3333

## Adding movies:

Edit `app/elm/MovieList.elm`, check that everything works, and open a PR.
